# 🎨 إعداد أيقونة التطبيق

## خطوات حفظ الصورة:

### 1️⃣ قم بحفظ الصورة الأساسية (الدمبل):
- **المسار:** `assets/icon/icon.png`
- **الحجم:** 1024×1024 بكسل (على الأقل)
- **الصيغة:** PNG مع خلفية شفافة (اختياري)
- **الاسم:** `icon.png`

### 2️⃣ قم بحفظ صورة الخلفية للـ Adaptive Icon:
- **المسار:** `assets/icon/icon_foreground.png`
- **الحجم:** 1024×1024 بكسل
- **الصيغة:** PNG
- **الاسم:** `icon_foreground.png`
- **الملاحظة:** هذه الصورة ستكون في مقدمة اللون الأزرق

## الخطوات التالية:

### 3️⃣ تشغيل الأمر:
```bash
flutter pub get
flutter pub run flutter_launcher_icons
```

### ماذا سيحدث؟
✅ سيتم إنشاء أيقونات بجميع الأحجام المطلوبة تلقائياً
✅ سيتم تحديث `AndroidManifest.xml` تلقائياً
✅ سيتم تحديث `Info.plist` لـ iOS تلقائياً

## أحجام الأيقونات التي سيتم إنشاؤها:

### Android:
- `mipmap-mdpi/ic_launcher.png` - 48×48
- `mipmap-hdpi/ic_launcher.png` - 72×72
- `mipmap-xhdpi/ic_launcher.png` - 96×96
- `mipmap-xxhdpi/ic_launcher.png` - 144×144
- `mipmap-xxxhdpi/ic_launcher.png` - 192×192

### Adaptive Icon (Android 8+):
- `mipmap-*dpi/ic_launcher_foreground.png`

## 🎯 ملخص الإعدادات:

```yaml
flutter_launcher_icons:
  android: true          # تفعيل Android
  ios: true             # تفعيل iOS
  image_path: "assets/icon/icon.png"  # الصورة الأساسية
  adaptive_icon_background: "#1E88E5"  # اللون الأزرق (تطبيقك)
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
```

## 📝 ملاحظات مهمة:

1. **الخلفية الشفافة:** الصورة الأساسية يفضل أن تكون بخلفية شفافة
2. **الـ Adaptive Icon:** هو مطلوب لـ Android 8.0+
3. **نسبة الأبعاد:** استخدم نسبة مربعة 1:1
4. **الجودة:** استخدم أعلى جودة ممكنة (192×192 على الأقل)

## ✅ التحقق من النتائج:

بعد تشغيل الأمر، ستجد الأيقونات في:
```
android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
...إلخ
```

## 🚀 بعد حفظ الصور:

```bash
flutter clean
flutter pub get
flutter pub run flutter_launcher_icons
flutter run
```

---
**✨ هذا كل شيء! الأيقونة ستظهر على الفور على جهازك الافتراضي**
